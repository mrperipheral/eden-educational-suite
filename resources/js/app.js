import Alpine from 'alpinejs';

/*
 |--------------------------------------------------------------------------
 | Alpine.js
 |--------------------------------------------------------------------------
 |
 | Alpine powers the small amount of client-side interactivity the app
 | shell needs (mobile navigation drawer, dropdown menus, confirmation
 | dialogs). It is intentionally the only JavaScript framework in the
 | project. Do not introduce React / Vue / Inertia without an explicit
 | decision recorded in docs/architecture.md.
 |
 */

window.Alpine = Alpine;

Alpine.start();
